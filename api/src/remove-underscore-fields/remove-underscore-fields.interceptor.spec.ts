import { RemoveUnderscoreFieldsInterceptor } from './remove-underscore-fields.interceptor';

describe('RemoveUnderscoreFieldsInterceptor', () => {
  it('should be defined', () => {
    expect(new RemoveUnderscoreFieldsInterceptor()).toBeDefined();
  });
});
